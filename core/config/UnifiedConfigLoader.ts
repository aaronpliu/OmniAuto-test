/**
 * UnifiedConfigLoader — Core wrapper
 *
 * Phase 1: Re-export from existing framework/utils/unifiedConfig.ts
 * Phase 5: Move implementation here (must stay in core — shared by all plugins)
 */
export { UnifiedConfigLoader, unifiedConfig } from "../../framework/utils/unifiedConfig";
